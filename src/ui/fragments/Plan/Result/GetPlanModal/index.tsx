import Button from '@/ui/components/Button'

export default function GetPlanModal(props: { children: React.ReactNode }) {
  return (
    <Button ellipsed onClick={() => {}}>{props.children}</Button>
  )
}